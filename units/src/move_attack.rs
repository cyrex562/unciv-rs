use map::map::TileId;
use crate::unit::Unit;

pub fn attack_move(unit: &mut Unit, targetLoc: TileId)  {
    // conduct a move and attack action against a target at the given location. 
    // TODO: get the unit's location, from the unit instance.
    // TODO: queue a multi-turn order
    unimplemented!()
}
